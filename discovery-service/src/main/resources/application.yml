spring:
  application:
    name: eureka-discovery-service

server:
  port: 8094
eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://localhost:8094/eureka
